(function() {
    "use strict";
    $("document").ready(init);
    
    function init(e) {
        $("addstyle").click(ajouterStyle);
        $("addparoles").click(ajouterParoles);
    }
    
    function ajouterStyle(e) {
        
    }
}());